---
phase: 02-agent-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/index.ts
  - src/agents/AgentNode.ts
  - src/adapters/index.ts
  - src/adapters/MockAdapter.ts
  - src/adapters/GeminiAdapter.ts
autonomous: true
requirements:
  - BACK-03
  - BACK-04
  - BACK-05
  - BACK-06
  - ORCH-03
must_haves:
  truths:
    - AgentNode runtime listens for step assignments
    - AgentNode triggers AI execution when step assigned
    - Mock adapter returns deterministic output
    - Gemini adapter calls real API
    - Escrow locks on task creation, releases on completion
  artifacts:
    - path: src/agents/AgentNode.ts
      provides: AgentNode runtime class
    - path: src/adapters/MockAdapter.ts
      provides: Mock deterministic AI adapter
    - path: src/adapters/GeminiAdapter.ts
      provides: Gemini API adapter
---

<objective>
Implement AgentNode runtime and AI adapters for Phase 2.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Implement AgentNode runtime</name>
  <files>src/agents/AgentNode.ts</files>
  <action>
Create AgentNode class that:
1. Listens for step assignments from contract events
2. Triggers AI execution when step assigned
3. Generates proof of execution
4. Submits attestation transaction

Key methods:
- constructor(config: AgentNodeConfig)
- async start(): void - start listening for events
- async stop(): void - stop listening
- async executeStep(taskId: string, stepIndex: number): Promise<void>
- private async generateProof(output: any): Promise<string>
- private async submitAttestation(taskId, stepIndex, proof): Promise<void>
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>AgentNode can listen and execute</done>
</task>

<task type="auto">
  <name>Implement Mock AI Adapter</name>
  <files>src/adapters/MockAdapter.ts</files>
  <action>
Create MockAdapter class:
- Returns deterministic, structured output
- Simulates different agent capabilities
- Input: prompt, context, capability
- Output: { result: string, confidence: number, metadata: object }
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Mock adapter returns predictable output</done>
</task>

<task type="auto">
  <name>Implement Gemini API Adapter</name>
  <files>src/adapters/GeminiAdapter.ts</files>
  <action>
Create GeminiAdapter class:
- Uses Gemini API for real AI execution
- Handles API authentication
- Converts responses to structured format
- Input: prompt, context
- Output: { result: string, confidence: number, metadata: object }
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Gemini adapter ready for API key</done>
</task>

<task type="auto">
  <name>Update adapters index</name>
  <files>src/adapters/index.ts</files>
  <action>
Export both adapters with factory functions:
- createMockAdapter(): MockAdapter
- createGeminiAdapter(apiKey: string): GeminiAdapter
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Adapters exported properly</done>
</task>

<task type="auto">
  <name>Update agents index</name>
  <files>src/agents/index.ts</files>
  <action>
Export AgentNode and helper functions
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Agent module exports complete</done>
</task>

</tasks>

<verification>
- [ ] AgentNode compiles
- [ ] Mock adapter works
- [ ] Gemini adapter ready
- [ ] Escrow integrated with orchestrator
</verification>

<output>
After completion, create `.planning/phases/02-agent-runtime/02-01-SUMMARY.md`
