---
phase: 01-smart-contracts
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crypto/index.ts
  - src/crypto/deterministic.ts
  - src/crypto/hash.ts
  - src/crypto/signature.ts
  - src/storage/index.ts
  - src/orchestrator/index.ts
autonomous: true
requirements:
  - BACK-07
  - BACK-08
  - BACK-09
  - BACK-10
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - Deterministic JSON serialization produces consistent output
    - SHA256 hashing produces verifiable hashes
    - Ed25519 signature verification works correctly
    - Persistent storage saves and retrieves data
    - Orchestrator can validate DAG structure
  artifacts:
    - path: src/crypto/index.ts
      provides: Crypto module exports
    - path: src/crypto/deterministic.ts
      provides: Canonical JSON serialization
    - path: src/crypto/hash.ts
      provides: SHA256 hashing utilities
    - path: src/crypto/signature.ts
      provides: Ed25519/Midnight signature verification
    - path: src/storage/index.ts
      provides: Persistent storage interface
    - path: src/orchestrator/index.ts
      provides: Orchestrator client with DAG validation
  key_links:
    - from: src/crypto/deterministic.ts
      to: src/crypto/hash.ts
      via: deterministic serialization feeds into hash
    - from: src/orchestrator/index.ts
      to: src/storage/index.ts
      via: orchestrator uses storage for task state
---

<objective>
Implement cryptographic utilities (deterministic JSON, SHA256, Ed25519) and persistent storage layer, plus basic orchestrator with DAG validation.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-smart-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement cryptographic utilities</name>
  <files>src/crypto/index.ts, src/crypto/deterministic.ts, src/crypto/hash.ts, src/crypto/signature.ts</files>
  <action>
Create crypto utilities:

**src/crypto/deterministic.ts:**
- Use canonical-json library
- Export function: canonicalStringify(obj: any): string
- Handles special values, key ordering, whitespace consistently

**src/crypto/hash.ts:**
- Use Node.js crypto module
- Export functions:
  - sha256(data: string | Buffer): string (hex)
  - sha256Batch(items: string[]): string[] (hex)
  - hashObject(obj: any): string (canonical stringify + sha256)

**src/crypto/signature.ts:**
- Use Node.js crypto module (verify with ed25519)
- Export functions:
  - verifySignature(publicKey: string, message: string, signature: string): boolean
  - verifyMidnightSignature(publicKey: Uint8Array, message: Uint8Array, signature: Uint8Array): boolean
- Support both hex and Uint8Array formats

**src/crypto/index.ts:**
- Re-export all crypto functions
- Document usage
  </action>
  <verify>
npx tsc --noEmit && node -e "require('./src/crypto')"
  </verify>
  <done>
All crypto utilities compile and are importable
  </done>
</task>

<task type="auto">
  <name>Implement persistent storage</name>
  <files>src/storage/index.ts</files>
  <action>
Create storage layer using classic-level:

**src/storage/index.ts:**
- Use classic-level for persistent storage
- Export class Storage:
  - constructor(dbPath: string)
  - async open(): void
  - async close(): void
  
  // Task storage
  - async saveTask(taskId: string, task: Task): Promise<void>
  - async getTask(taskId: string): Promise<Task | null>
  - async listTasks(): Promise<Task[]>
  - async deleteTask(taskId: string): Promise<void>
  
  // Agent storage
  - async saveAgent(agentId: string, agent: Agent): Promise<void>
  - async getAgent(agentId: string): Promise<Agent | null>
  - async listAgents(): Promise<Agent[]>
  
  // Config storage
  - async saveConfig(key: string, value: any): Promise<void>
  - async getConfig(key: string): Promise<any>
  
  // Encrypted outputs storage
  - async saveEncryptedOutput(taskId: string, stepIndex: number, encrypted: Buffer): Promise<void>
  - async getEncryptedOutput(taskId: string, stepIndex: number): Promise<Buffer | null>

Use JSON serialization for complex objects
  </action>
  <verify>
npx tsc --noEmit
  </verify>
  <done>
Storage layer compiles and creates database files
  </done>
</task>

<task type="auto">
  <name>Implement orchestrator with DAG validation</name>
  <files>src/orchestrator/index.ts</files>
  <action>
Create orchestrator client:

**src/orchestrator/index.ts:**
- Import client from ../client
- Import storage from ../storage
- Import crypto from ../crypto

Export class Orchestrator:
- constructor(config: OrchestratorConfig)
- async start(): Promise<void>
- async stop(): Promise<void>

**DAG Validation (ORCH-02):**
- validateDAG(dag: DAG): ValidationResult
  - Check for cycles using topological sort (tsort)
  - Validate all dependencies exist
  - Validate step indices are valid
  - Validate no duplicate steps
  - Return detailed error messages

**Task Management:**
- async createTask(dag: DAG, escrow: number, deadline: number): Promise<TaskId>
- async assignStep(taskId: string, stepIndex: number, agentId: string): Promise<void>
- async getTaskStatus(taskId: string): Promise<TaskStatus>

**Escrow Management (ORCH-03):**
- lockEscrow(taskId: string, amount: number): Promise<void>
- releaseEscrow(taskId: string): Promise<void>
- slashEscrow(taskId: string, amount: number): Promise<void>
  </action>
  <verify>
npx tsc --noEmit
  </verify>
  <done>
Orchestrator compiles with DAG validation functional
  </done>
</task>

</tasks>

<verification>
- [ ] All crypto utilities compile
- [ ] Storage layer creates and manages database
- [ ] DAG validation detects cycles and invalid structures
- [ ] Escrow management functions are implemented
</verification>

<success_criteria>
- [ ] Deterministic JSON works correctly
- [ ] SHA256 hashing produces verifiable output
- [ ] Ed25519 verification works with test vectors
- [ ] Storage persists data across restarts
- [ ] DAG validation passes valid graphs, rejects cyclic ones
</success_criteria>

<output>
After completion, create `.planning/phases/01-smart-contracts/01-03-SUMMARY.md`
</output>
