---
phase: 01-smart-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/index.ts
  - src/types/index.ts
autonomous: true
requirements:
  - BACK-01
  - BACK-02
must_haves:
  truths:
    - TypeScript project compiles without errors
    - All modules can import each other correctly
    - Basic project structure is functional
  artifacts:
    - path: package.json
      provides: Node.js project configuration with all dependencies
    - path: tsconfig.json
      provides: TypeScript configuration
    - path: src/index.ts
      provides: Main entry point
    - path: src/types/index.ts
      provides: TypeScript type definitions
  key_links:
    - from: src/index.ts
      to: src/types/
      via: import statements
---

<objective>
Set up the TypeScript project structure for BlindSwarm with proper dependencies and module organization.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/01-smart-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize TypeScript project</name>
  <files>package.json, tsconfig.json</files>
  <action>
1. Initialize npm project if package.json doesn't exist
2. Install dependencies:
   - typescript: ^5.3.0
   - @midnight-ntwrk/midnight-js: ^4.0.2
   - @midnight-ntwrk/compact-runtime: (latest compatible)
   - @midnight-ntwrk/midnight-js-wallet: (latest)
   - canonical-json: ^0.1.0
   - classic-level: ^3.0.0
   - zod: ^3.22.0 (for validation)
   - types/node: ^20.0.0 (dev)
   - typescript: ^5.3.0 (dev)
3. Configure tsconfig.json with:
   - target: ES2022
   - module: NodeNext
   - moduleResolution: NodeNext
   - outDir: ./dist
   - rootDir: ./src
   - strict: true
   - esModuleInterop: true
  </action>
  <verify>
npm install && npx tsc --noEmit
  </verify>
  <done>
Project compiles without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Create directory structure and entry points</name>
  <files>src/index.ts, src/types/index.ts</files>
  <action>
Create the following directory structure:
- src/
  - index.ts (main entry point, exports everything)
  - types/index.ts (all TypeScript interfaces)
  - contracts/ (Midnight contract code)
  - client/ (Midnight.js client wrappers)
  - crypto/ (cryptographic utilities)
  - storage/ (persistent storage)
  - orchestrator/ (orchestration logic)

Create src/index.ts that exports all modules
Create src/types/index.ts with core interfaces:
- Agent type
- Task type
- TaskStep type
- Attestation type
- Dispute type
  </action>
  <verify>
npx tsc --noEmit should pass
  </verify>
  <done>
All TypeScript files compile and exports work
  </done>
</task>

</tasks>

<verification>
- [ ] npm install completes without errors
- [ ] TypeScript compiles without errors
- [ ] All modules can be imported from src/index.ts
</verification>

<success_criteria>
- [ ] Package.json has all required dependencies
- [ ] TypeScript compiles successfully
- [ ] Project structure is in place for next plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-smart-contracts/01-01-SUMMARY.md`
</output>
